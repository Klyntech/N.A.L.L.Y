# Security — guide pointer

The canonical security policy lives at the repository root: [`SECURITY.md`](../../SECURITY.md).

It covers the permission gate (`allow` / `ask` / `deny`), authentication,
Fernet-encrypted token storage, tool execution safety, input validation,
rate limiting, and network security. Start there; this file exists only so
the guides index is complete without duplicating policy.
