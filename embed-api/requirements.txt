# Embed API — Free tier separate instance (ONNX runtime, no torch, no sentence-transformers)
# Target: <400MB image, fits Render Free 512MB, 120MB RAM at runtime.
# ONNX quantized MiniLM = 22MB model file, no torch dependency.

fastapi==0.110.0
uvicorn[standard]==0.29.0
httpx==0.27.0
pydantic==2.6.0

# ONNX runtime — 120MB RAM, no torch needed
onnxruntime==1.17.0
transformers==4.38.0
optimum[onnxruntime]==1.17.0
tokenizers==0.15.2
numpy==1.26.0

# NO torch, NO sentence-transformers — model is baked via ONNX at build time
