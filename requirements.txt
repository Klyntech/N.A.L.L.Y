# Nally Requirements

# Terminal UI
rich>=13.0
pyfiglet>=1.0

# Web framework
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.0

# LLM
openai>=1.0.0
python-dotenv>=1.0.0

# Agent framework
langgraph>=0.2.0
langchain-core>=0.3.0
langgraph-checkpoint-sqlite>=2.0.0

# Token counting
tiktoken>=0.7.0

# System
psutil>=5.9.0
requests>=2.31.0
Pillow>=10.0.0
numpy>=1.24.0

# Windows audio control (optional, Windows-only)
# pycaw>=20230407; sys_platform == 'win32'
# comtypes>=1.4.0; sys_platform == 'win32'

# MCP (Model Context Protocol)
mcp>=1.27,<2

# Token encryption
cryptography>=41.0.0

# Web search
duckduckgo-search>=7.0.0
httpx>=0.27.0

# Telegram bot
python-telegram-bot>=20.0

# Voice I/O (push-to-talk mode)
faster-whisper>=1.0.0
piper-tts>=1.3.0
sounddevice>=0.4.6
keyboard>=0.13.5

# VoIP (zero-cost SIP phone via LiveKit Cloud free tier)
livekit-agents>=0.1.0
livekit-plugins-silero>=0.1.0
livekit-plugins-deepgram>=0.1.0
livekit-plugins-elevenlabs>=0.1.0

# Database (optional, for PostgreSQL backend)
asyncpg>=0.29.0

# Redis (optional, for caching layer)
redis>=5.0.0

# Dev dependencies (install with: pip install -r requirements.txt -e ".[dev]")
# Or directly: pip install pytest pytest-cov ruff pre-commit
