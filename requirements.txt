# Nally Requirements

# Terminal UI
rich>=13.0
pyfiglet>=1.0

# Web framework
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.0

# LLM
openai>=1.0.0
python-dotenv>=1.0.0

# Agent framework
langgraph>=0.2.0
langchain-core>=0.3.0
langgraph-checkpoint-sqlite>=2.0.0

# Token counting
tiktoken>=0.7.0

# System
psutil>=5.9.0
requests>=2.31.0
Pillow>=10.0.0
numpy>=1.24.0

# Windows audio control (used by agent/router.py volume control; Windows-only)
pycaw>=20230407; sys_platform == 'win32'
comtypes>=1.4.0; sys_platform == 'win32'

# Fetch tool readability extraction (nally/tools/fetch.py; has fallback if absent)
readability-lxml>=0.8

# Window focus control (nally/agent/router.py)
pygetwindow>=0.0.9

# Telethon — Telegram user account (nally/telegram/user.py)
# Explicit so it's always available.
telethon>=1.34

# MCP (Model Context Protocol)
mcp>=1.27,<2

# Token encryption
cryptography>=41.0.0

# Web search
duckduckgo-search>=7.0.0
httpx>=0.27.0

# Curiosity / proactive learning
feedparser>=6.0.0

# Telegram bot
python-telegram-bot>=20.0

# Database — Postgres (Neon/Supabase) + optional asyncpg
psycopg[binary]>=3.1
asyncpg>=0.29.0

# Turso/LibSQL (persistent memory on Render) — optional, only needed if TURSO_URL is set
# libsql-experimental>=0.0.34  # Disabled: requires Rust compilation, fails on Render read-only fs
# If using Turso, install separately: pip install libsql-experimental

# Cloudflare R2 (persistent file storage)
boto3>=1.28.0

# Redis (optional, for caching layer)
redis>=5.0.0

# Observability
websockets>=12.0
opentelemetry-api>=1.20
opentelemetry-sdk>=1.20
opentelemetry-exporter-prometheus
prometheus-client>=0.19

# Dev dependencies (install with: pip install -r requirements.txt -e ".[dev]")
# Or directly: pip install pytest pytest-cov ruff pre-commit
